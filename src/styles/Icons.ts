import {StyleSheet} from 'react-native';

export const style = StyleSheet.create({
    container: {
      paddingRight: 5,
    },
    shareIcon: {
      fontSize: 24,
      color: '#000',
    },
  });