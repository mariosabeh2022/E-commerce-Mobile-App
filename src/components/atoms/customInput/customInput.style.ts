import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  input: {
    width: '50%',
    color: 'black',
    borderColor: 'black',
    borderWidth: 1,
    borderRadius: 5,
    padding: 10,
  },
});
